# Calculator

Простой калькулятор на Python с операциями сложения и вычитания.

Проверка:

```bash
python -c "from calculator import add, subtract; assert add(2, 3) == 5; assert subtract(5, 2) == 3"
```

