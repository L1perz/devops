# Calculator

РџСЂРѕСЃС‚РѕР№ РєР°Р»СЊРєСѓР»СЏС‚РѕСЂ РЅР° Python СЃ РѕРїРµСЂР°С†РёСЏРјРё СЃР»РѕР¶РµРЅРёСЏ Рё РІС‹С‡РёС‚Р°РЅРёСЏ.

РџСЂРѕРІРµСЂРєР°:

```bash
python -c "from calculator import add, subtract; assert add(2, 3) == 5; assert subtract(5, 2) == 3"
```


Дополнительная документация.

Дополнительная документация.
